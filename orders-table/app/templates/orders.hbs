{{outlet}}

<div class="page-content">
  {{#link-to "orders.add"}}
    <button type="button">Добавить заказ</button>
  {{/link-to}}

  <h3>Таблица заказов</h3>

  <div class="form-group">
    {{input class="filter" type="text" autofocus=true value=filter placeholder="Отфильтровать по имени"}}
  </div>

  {{orders-list filteredOrders=filteredOrders actionToCall="updateQueryParams" sortBy=sortBy sortDir=sortDir}}
</div>
